import { SidebarPositionDirective } from './sidebar-position.directive';

describe('SidebarPositionDirective', () => {
  it('should create an instance', () => {
    const directive = new SidebarPositionDirective();
    expect(directive).toBeTruthy();
  });
});
